package com.corun.faultcheck.fault.check.manager;

import com.corun.faultcheck.fault.check.modelVO.FaultListInfo;

/**
 * 创建缺陷信息
 */
public interface CreateFaultManger {
    void createFaultInfo(FaultListInfo faultListInfo);

}
